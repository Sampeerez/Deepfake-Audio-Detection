# Dependencias del Banco de Pruebas Anti-Spoofing (TFG)
# Instalación en Linux:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# Despliegue en Streamlit Community Cloud (solo CPU): este archivo basta para
# que el servidor instale todo. La rueda por defecto de `torch` incluye CUDA y
# es pesada; si el despliegue falla por límite de recursos, fija la rueda CPU
# DESCOMENTANDO las dos líneas siguientes (NO lo hagas en tu equipo local con
# GPU, instalaría torch sin CUDA):
#   --extra-index-url https://download.pytorch.org/whl/cpu
#   torch>=2.0
numpy>=1.24
scipy>=1.10
PyYAML>=6.0
librosa>=0.10
soundfile>=0.12
PyWavelets>=1.4
scikit-learn>=1.3
joblib>=1.3                   # load exported classic models (XGBoost) in the demo
xgboost>=2.0
torch>=2.0
pytest>=7.0
streamlit>=1.30
matplotlib>=3.7
imageio-ffmpeg>=0.4.9
streamlit-sortables>=0.2     # drag-to-reorder sources in Signal Explorer
altair>=5.0                  # interactive charts (leaderboard, CNN curves)
pandas>=2.0                  # results tables / dataframes